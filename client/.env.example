# Back-end API base URL (include /api when the server is mounted behind a prefix).
# Local development defaults to http://localhost:8080/api when this variable is omitted.
VITE_API_URL=http://localhost:8080/api

# Production/staging example:
# VITE_API_URL=https://api-staging.<domeniu>.com/api